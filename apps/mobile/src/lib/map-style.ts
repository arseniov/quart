// OSM raster tiles via MapLibre demo style. Production should self-host
// a demotiles.maptiler.com-mirror or use the Quart style endpoint.
// ponytail: demotiles CDN is acceptable for dev; switch to self-hosted before first EAS prod build
export const osmStyle = 'https://demotiles.maplibre.org/style.json';
